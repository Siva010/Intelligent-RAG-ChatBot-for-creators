fastapi>=0.100.0 # build the backend apis
uvicorn>=0.22.0 # asgi server that runs the fast api
pydantic>=2.0 # validates and structures the data
pydantic-settings>=2.0 # load the environment variables
youtube-transcript-api>=0.6.1 # fetch youtube video transcripts
yt-dlp>=2023.7.6 # extracts the metadata from youtube 
types-yt-dlp>=2026.3.17 
sse-starlette>=1.6.1 # enables real time response streaming using SSE
langchain>=0.1.0 # provides tools for llm applications
langgraph>=0.0.26 # manages AI workflow 
langchain-openai>=0.0.2 # connect open ai model
langchain-google-genai>=1.0.0 # conncet to gemini models 
google-genai>=1.0.0 #sdk accesing api
redis>=5.0.0 # memory caching
pinecone>=5.4.2 # stores and retrieves vector embeddings for rag
python-dotenv>=1.0.0 #load the enviromnent variables
pytest>=7.0.0 # used for testing
pytest-asyncio>=0.23.0 # async test support for astream_session / SSE generators
httpx>=0.27.0 # required by FastAPI TestClient for async routes
slowapi>=0.1.9 # rate limiting
langgraph-checkpoint-redis>=0.4.1 # redis persistence for langgraph
tenacity>=8.5.0 # exponential backoff for network calls
apify-client>=1.6.4 # apify SDK for scraping fallbacks
celery>=5.3.0 # distributed task queue for background analysis jobs
numpy>=1.24.0 # array ops for mock embedding fallback in vector_store
aiosqlite>=0.19.0 # async SQLite driver for LangGraph checkpoint persistence
