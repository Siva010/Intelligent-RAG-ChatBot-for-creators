[pytest]
# Discover tests in both the tests/ package and the root-level legacy scripts
testpaths = tests
asyncio_mode = auto

# Short summary: show all passed/failed/skipped in one line
addopts = -v --tb=short -q
